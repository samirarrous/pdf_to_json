# pdf_to_json
Prototype d’extraction de données structurées à partir de PDF. L’application extrait le texte (PDF natif ou OCR), utilise un petit LLM pour identifier les informations demandées et génère un JSON conforme au schéma fourni par l’utilisateur via une interface web simple. 
